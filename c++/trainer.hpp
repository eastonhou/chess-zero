#include "models.hpp"
#include <torch/torch.h>

class Trainer {
private:
    model_t _model;

public:
    Trainer() {

    }

    void run() {
        while (true) {
            //auto train_data = play();
        }
    }
};